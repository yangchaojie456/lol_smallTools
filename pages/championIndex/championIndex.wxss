.champion-index-home{
  width: 100%;
  height: 100vh;
  background-size: cover;
  background-position: top center;
  background-image: url('https://personal-tailor.vip/images/LOL/bg.jpg');
  padding:10rpx;
  display: flex;
  justify-content: space-between;
  font-size: 26rpx;
  color:white;
}
.champion-index-home-left{
  width: 200rpx;
  
}
.champion-index-home-right{
  width: 520rpx;
  
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  align-content: flex-start;
}
.champion-index-search input{
  line-height: 26rpx;
  border: 2px solid #6f5929;
  padding-left: 0.5em;
  color:white;
  margin: 0rpx 0 40rpx;
}
.champion-index-search-placeholder{
  color:white;
}
.champion-index-filter-sort{
  width: 156rpx;
  height: 30rpx;
  line-height: 30rpx;
  background: #101820;
}
.hr{
  height: 0;
  border: 1px solid #182329;
  margin-bottom: 20rpx;
}
.current-item{
  background: #1e2328;
}

.champion-index-home-item{
  width: 167rpx;
  height: 167rpx;
  background: gray;
  margin-bottom: 10rpx;
}

/*  重写 checkbox 样式  */
/* 未选中的 背景样式 */
checkbox .wx-checkbox-input{
   border-radius: 0;/* 圆角 */
   width: 20rpx; /* 背景的宽 */
   height: 20rpx; /* 背景的高 */
   margin-bottom: 6rpx;
   border: 2rpx solid #62573c;
   background: transparent;
}
.currentItem checkbox .wx-checkbox-input{
   border-radius: 0;/* 圆角 */
   width: 20rpx; /* 背景的宽 */
   height: 20rpx; /* 背景的高 */
   margin-bottom: 6rpx;
   border: 2rpx solid #62573c;
   background: #5f5325;
}

/* 选中后的 背景样式 （红色背景 无边框 可根据UI需求自己修改） */
checkbox .wx-checkbox-input.wx-checkbox-input-checked{
   border: 2rpx solid #7c6128;   
   /* background: #f6de14; */
}
/* 选中后的 对勾样式 （白色对勾 可根据UI需求自己修改） */
checkbox .wx-checkbox-input.wx-checkbox-input-checked::before{
   border-radius: 50%;/* 圆角 */
   width: 40rpx;/* 选中后对勾大小，不要超过背景的尺寸 */
   height: 40rpx;/* 选中后对勾大小，不要超过背景的尺寸 */
   line-height: 40rpx;
   text-align: center;
   font-size:30rpx; /* 对勾大小 30rpx */
   color:#f6de14; /* 对勾颜色 白色 */
   background: transparent;
   transform:translate(-50%, -50%) scale(1);
   -webkit-transform:translate(-50%, -50%) scale(1);
}
